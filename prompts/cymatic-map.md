---
description: "Alias for /pam — Project Architectural Maps for CymaticAPPS."
---

# /cymatic-map

This is an alias for `/pam`. Load and follow the PAM skill at `~/.pi/agent/skills/pam/SKILL.md`.

Read PAM's canonical identity first: `~/.pi/agent/agents/PAM.md`.

## Arguments

The text that followed `/cymatic-map` (if any): **{{ARGS}}**

Forward exactly as if `/pam {{ARGS}}` had been invoked.
